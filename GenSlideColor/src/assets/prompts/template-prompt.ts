/**
 * ============================================
 * 模板参考提示词片段
 * ============================================
 * 
 * 用途：当用户选择了 HTML 模板时，追加到主提示词中
 * 使用位置：src/keepstyle/generateService.ts - buildTemplateReferencePrompt()
 * 
 * 说明：
 * - 这些是提示词片段，不是完整提示词
 * - 会被追加到主提示词的末尾
 * - 用于强制模型参考模板的布局结构
 * 
 * 占位符：
 * - {^htmlTemplate^}  HTML 模板代码
 * - {^width^}         页面宽度
 * - {^height^}        页面高度
 */

/**
 * 模板参考提示词（追加在主提示词后面）
 * 用于非样式提取模式（image_reference / text 模式）
 */
export const TEMPLATE_REFERENCE_PROMPT = `

---

## 🚨🚨🚨 【最高优先级】HTML 布局模板（必须严格遵守）🚨🚨🚨

以下是用户选择的 HTML 布局模板。**这是你生成页面的骨架和结构基础**。

**分工明确**：
- **布局结构 → 严格按照下面的模板**（DOM 结构、嵌套层级、容器数量、区块划分）
- **视觉样式 → 参考用户输入或参考图片**（颜色、渐变、阴影、字体）

\`\`\`html
{^htmlTemplate^}
\`\`\`

### ⚠️ 模板使用规则（70%保留 + 30%灵活调整）

#### 1. 结构层面（70% 必须保留，30% 可微调）

**必须保留的核心结构（不可修改）**：
- ✅ 模板的主要布局框架（header、main、footer、section 的分区方式）
- ✅ 模板的整体布局方向（flex-row/flex-col、grid 列数等）
- ✅ 核心容器的嵌套层级关系

**允许的微调（30%以内）**：
- ✅ 可以**添加**少量新元素（如额外的装饰图标、分隔线）
- ✅ 可以**合并或拆分**个别文本框
- ✅ 可以**调整**单个卡片/区块的内部布局
- ✅ 可以**增减**列表项数量以适配实际内容

**严禁的大幅修改**：
- ❌ 不得删除模板的主要区块
- ❌ 不得完全重构布局
- ❌ 不得改变模板的整体视觉骨架

#### 2. 尺寸层面（严格锁定）
- 页面必须精确适配 **{^width^}px × {^height^}px**
- 设置 \`body { overflow: hidden !important; }\`
- **绝对不允许出现任何方向的滚动条**

#### 3. 样式层面（完全自由替换）
- ✅ 所有颜色、字体、阴影、圆角 → 完全替换为参考图片/用户输入的视觉风格
- ✅ 可以添加装饰元素（z-index 低于内容）

#### 4. 内容层面（按主题创意填充）
- 将模板中的占位文字替换为符合用户主题的实际内容
- 可以根据内容需要调整文字长度和字号

---

`;


/**
 * 模板严格规则提示词（追加在样式提取模式的提示词后面）
 * 包含更严格的 DOM 结构保持规则
 */
export const TEMPLATE_STRICT_RULES_PROMPT = `

---

## 🚨🚨🚨 【最高优先级】HTML 布局模板（必须严格遵守）🚨🚨🚨

以下是用户选择的 HTML 布局模板。**这是你生成页面的骨架和结构基础**。

**分工明确**：
- **布局结构 → 严格按照下面的模板**（DOM 结构、嵌套层级、容器数量、区块划分）
- **视觉样式 → 参考上面的【风格样式参数】**（颜色、渐变、阴影、字体、材质）

\`\`\`html
{^htmlTemplate^}
\`\`\`

### ⚠️ 模板使用规则（70%保留 + 30%灵活调整）

#### 1. 结构层面（70% 必须保留，30% 可微调）

**必须保留的核心结构（不可修改）**：
- ✅ 模板的主要布局框架（header、main、footer、section 的分区方式）
- ✅ 模板的整体布局方向（flex-row/flex-col、grid 列数等）
- ✅ 核心容器的嵌套层级关系

**允许的微调（30%以内）**：
- ✅ 可以**添加**少量新元素（如额外的装饰图标、分隔线）
- ✅ 可以**合并或拆分**个别文本框（如将两行标题合并为一行）
- ✅ 可以**调整**单个卡片/区块的内部布局
- ✅ 可以**增减**列表项数量以适配实际内容

**严禁的大幅修改**：
- ❌ 不得删除模板的主要区块（如删掉整个 header 或 footer）
- ❌ 不得完全重构布局（如将横向布局改为纵向）
- ❌ 不得改变模板的整体视觉骨架

#### 2. 尺寸层面（严格锁定）
- 页面必须精确适配 **{^width^}px × {^height^}px**
- 设置 \`body { overflow: hidden !important; }\`
- **绝对不允许出现任何方向的滚动条**
- 如果内容过多，**必须减小字号或精简内容**，严禁通过滚动解决

#### 3. 样式层面（完全自由替换）
- ✅ 模板中的颜色 → 完全替换为【风格样式参数】中的色值
- ✅ 模板中的字体 → 完全替换为【风格样式参数】中的字体
- ✅ 模板中的阴影/圆角/渐变 → 完全替换为【风格样式参数】中的视觉效果
- ✅ 可以添加模板中没有的装饰元素（但 z-index 必须低于内容）

#### 4. 内容层面（按主题创意填充）
- 将模板中的占位文字替换为符合【用户主题】的实际内容
- 可以根据内容需要调整文字长度和字号
- 如有图表，保持图表位置，可调整图表类型和数据

---

`;

