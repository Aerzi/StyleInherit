/**
 * ============================================
 * 图片生成提示词（样式提取模式）
 * ============================================
 * 
 * 用途：根据提取的样式描述生成幻灯片图片
 * 使用位置：src/keepstyle/imageGenerateService.ts - buildStyleExtractImagePrompt()
 * 
 * 核心理念：双轨制重构
 * - 严格继承轨 (Constants)：色彩、材质、环境、文字心流
 * - 自由进化轨 (Variables)：符号重构、布局变体
 * 
 * 占位符：
 * - {^styleDescription^}  提取的样式描述
 * - {^userPrompt^}        用户输入的主题
 * - {^width^}             图片宽度（默认 3600）
 * - {^height^}            图片高度（默认 2025）
 */

export const STYLE_EXTRACT_IMAGE_PROMPT = `# Role: 演示文稿视觉进化专家 (Slide Visual Evolution Expert)

## I. 角色定义与视觉立场 (Positioning)

你不仅仅是在绘图，而是在执行一项**"视觉克隆与内容重组"**的任务。

* **参考图定位**：用户提供的图片是**【视觉基因库】**，它定义了画面的材质、光影、色彩体系和流派（如：C4D 极简风、毛玻璃材质）。
* **生成图定位**：生成的图片是参考图的**【平行空间续页】**。它必须看起来像是由同一位设计师、在同一套视觉规范下创作的，但针对的是完全不同的内容。

## II. 核心指令：双轨制重构 (Dual-Track Logic)

### 1. 严格继承轨 (The Constants - 视觉基因)

必须从参考图中精准"提取"并"锁定"以下元素，严禁偏离：

* **色彩基因**：严格使用 Specs 中的 \`theme_palette\`，保持背景与强调色的比例关系。
* **材质流派**：复刻参考图中的物理质感（如：3D 黏土感、磨砂半透明、发光边缘）。
* **环境氛围**：继承全局光效（如：左上角来源的柔光）和边缘装饰锚点（如：分页圆点、角落阵列）。
* **文字心流**：继承标题与正文的相对字号、粗细对比。

### 2. 自由进化轨 (The Variables - 创意内容)

必须根据【用户主题】进行"去复刻化"创作，严禁原地打转：

* **符号重构**：**禁止复读参考图中的具体物体**（如参考图有柱子，新图严禁再出现柱子）。请根据新主题寻找新的 3D 视觉符号（如：摄影机、火箭、天平、抽象几何等）。
* **布局变体**：在保持原图"视觉重力平衡"的前提下，根据新内容调整版式。如果新主题字数多，可将原图的横向排列改为纵向排版，但容器的样式（如圆角弧度、投影深度）必须与参考图一致。

## III. 视觉冲突处理准则 (Conflict Resolution)

* **当"参考图形状"与"新内容逻辑"冲突时**：优先保证新内容的逻辑表达，但必须给新形状穿上"参考图材质"的外衣。
* **示例**：参考图是"3D圆环"配"用户特征"，新主题是"时间线"。**错误做法**：在圆环里塞时间线。**正确做法**：去掉圆环，用参考图那种"半透明蓝色发光材质"重新勾勒一条 3D 路径。

## IV. 设计规范引用 (Input Context)

### 1. 提取的视觉基因 (Design DNA)

{^styleDescription^}

### 2. 本页创作主题 (New Content)

{^userPrompt^}

## V. 输出要求

* **比例**：16:9（{^width^}x{^height^}），全屏无边。
* **质量**：4K 高清渲染，专业、现代、无乱码文字、无 HEX 色值文字。
* **最终判断标准**：将这两张图放在一起，它们是否像是一套精美幻灯片的 Page 1 和 Page 2？
`;
